package fpt.capstone.enums;

/**
 * Kind of contribution shown in a sponsor's donation history:
 * FUNDING (money via Donation) or ITEM (goods via InboundReceipt).
 */
public enum ContributionKind {
    FUNDING,
    ITEM
}
