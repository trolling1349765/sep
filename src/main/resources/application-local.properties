# Database Local
spring.datasource.url=jdbc:mysql://localhost:1435/test?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456

# Redis Local
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Chatbot service (Python sidecar; Spring runs on host, compose publishes 8001)
chatbot.service.url=http://localhost:8001
